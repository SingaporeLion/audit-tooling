# Tooling Philosophy

Good audit tooling should help answer:

- where are the risky state transitions?
- where do live assumptions differ from stored state?
- where do privileged actions affect supposedly permissionless flows?
- where do verification and execution assumptions diverge?

The best tooling reduces search space. It does not replace judgment.
